'use strict';


/**
  * Main module of the application.
 */
var app = angular
  .module('BotForFood', [
    'ngMaterial'
  ]);


app.run(['$rootScope', '$timeout',  '$compile',
  function ($rootScope, $timeout,  $compile) {



  }]); // end run()
